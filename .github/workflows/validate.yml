name: Validate Model Config

on:
  push:
    paths: ['models/**/*.json']
  pull_request:
    paths: ['models/**/*.json']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON
        run: python3 -c "import json; json.load(open('models/Prunnerai test v.1.0/config.json')); print('✅ Valid')"
